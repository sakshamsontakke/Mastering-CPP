#include <iostream>
using namespace std;

int main (){

    //comparision operator

    int a = 10 ,  b = 30;
    cout<<(a==b)<<endl;    //checking the value of variable are equal are not
    cout<<(a!=b)<<endl; // not equal to operator
    cout<<(a>=b)<<endl; //greater then equal to
    cout<<(a<=b)<<endl;//smaller than equal to 
    cout<<(a>b)<<endl;//greater than
    cout<<(a<b)<<endl;//smaller then 

    // the "0" represents false , the "1" represents true 
    return 0;
}