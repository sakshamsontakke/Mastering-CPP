#include <iostream>
using namespace std; 


// constant Argument :  As we use default argument it's default value is mutable but in case of constant argument we cannot chage it's value , if we does it will give error  

int strLength(const char *p){   //we usually use "const" with reference and pointers

}

int main(){ 

    cout<<"tutorial on Contant Arguments"<<endl;

    return 0;
}